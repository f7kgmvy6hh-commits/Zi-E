# Hermes integration

The server implements [PROTOCOL.md](PROTOCOL.md) against installed Hermes 0.20.6.
No Hermes source or credentials are vendored. Chat returns HTTP 503 until configured.
`smart_model_routing` is a setup placeholder and is not consumed here.

#pragma once

// Copy to secrets.h and fill locally.
// Never commit secrets.h.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// Cloud credentials should preferably live on the laptop/phone brain,
// not in distributable robot firmware.
#define API_KEY_PLACEHOLDER "DO_NOT_COMMIT_REAL_KEYS"
